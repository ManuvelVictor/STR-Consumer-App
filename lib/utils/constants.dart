class AppConstants {
  static const String appName = 'STR Customer App';
  static const double defaultPadding = 16.0;
  static const double defaultBorderRadius = 12.0;

  // Animation durations
  static const Duration shortAnimationDuration = Duration(milliseconds: 200);
  static const Duration normalAnimationDuration = Duration(milliseconds: 300);
  static const Duration longAnimationDuration = Duration(milliseconds: 500);

  // Scroll thresholds
  static const double appBarHideThreshold = 100.0;
  static const double bottomBarHideThreshold = 50.0;
}